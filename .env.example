# 本地构建部署时，如果你通过命令行进入项目目录执行 `docker compose up -d --build`，
# 一般不需要这个文件，因为 shell 会自动提供 PWD。

# NAS 直接拉 GitHub 镜像部署时，复制为 `.env` 并修改下面两项。
DOCKPILOT_IMAGE=ghcr.io/你的github用户名/dockpilot:latest
DOCKPILOT_HOST_PATH=/volume1/docker/dockpilot
DOCKPILOT_PORT=8088
